<h1>Press</h1>
<div class="row">
    <div class="col-md-12">
        <table class="table table-bordered table-striped">
            <thead>
            <tr>
                <th>Name</th>
                <th>Status</th>
                <th>Options</th>
            </tr>
            </thead>
            <tbody>
            <?php foreach($this->data as $item) : ?>
            <tr>
                <td><?php echo $item['title']; ?> | <?php echo $item['title_en']; ?></td>
                <td><?php echo $item['status'] == true ? 'enabled' : 'disabled' ?></td>
                <td>
                    <a href="/admin/press/edit?id=<?php echo $item['id']; ?>" class="btn btn-primary">edit</a>
                    <a href="/admin/press/delete?id=<?php echo $item['id']; ?>" class="jsDelete btn btn-danger">delete</a>
                </td>
            </tr>
            <?php endforeach; ?>
            </tbody>
        </table>
        <form action="/admin/press/create" method="post">
            <button type="submit" class="btn btn-primary">Add press</button>
        </form>
    </div>
</div>